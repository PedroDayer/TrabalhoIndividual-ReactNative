import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  title: {
    color: "#FBFBFB",
    fontSize: 20,
  },
  container: {
    // #101110
    backgroundColor: "red",
    height: "100%",
    alignItems: "center",
    paddingTop: 80,
  },
});
